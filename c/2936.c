#include <stdio.h>

    int main(){
    int curu, boi, boto, mapi, iara, tot;
    scanf("%d %d %d %d %d", &curu, &boi, &boto, &mapi, &iara);
    tot = (curu * 300) + (boi * 1500) + (boto * 600) + (mapi * 1000) + (iara * 150);
    printf("%d\n", tot + 225);

    return 0;

 }